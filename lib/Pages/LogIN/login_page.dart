import 'package:flutter/material.dart';
import 'package:mmet_me_fit/Pages/LogIN/body.dart';

class LogInPage extends StatelessWidget {
  const LogInPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
